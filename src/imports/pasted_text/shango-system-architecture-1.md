Phase 1: The Global Platform Infrastructure
This is the system shell. Everything here lives outside the specific active workspaces, anchoring the user's projects, configurations, and system defaults.

1. The Left Global Navigation Rail (Dashboard State Only)
⚡ SHANGO Identity Block: Firmly anchored at the top left. Clicking it always returns the user to the default launch hub.

📂 Forge Hub (Home): The main entry window where the core creation prompt and live atmosphere canvas live.

📁 Project Vault: A clean list view of all apps built on SHANGO. Includes sort filters: Last modified, Creation date, Active deployments, and Bandwidth utilization.

📦 Template Sandbox: Pre-scaffolded visual blueprints (e.g., E-commerce systems, FinTech ledgers, Logistics dashboards) that can be instantly activated.

🔌 Ecosystem Keys (Connectors): The centralized panel to link third-party live services without touching code. (e.g., Paystack/Flutterwave APIs, Supabase databases, Twilio SMS gateways).

⚙️ Platform Settings: Account tier details, region hosting configurations, global light/dark system overrides, and team seat management.

2. The Project Vault & Template Hub Box (Lower Scroll Canvas)
Horizontal System Header:

Left side tabs: [🔍 Search Project Name], [Active Projects], [Archived], [System Blueprints].

Right side button: [Browse All Modules →].

Horizontal Card Tracks: Minimalist, translucent #131314 container cards that glide horizontally on scroll. Each card contains:

Project thumbnail preview.

Project label name (e.g., Lagos Health Portal).

Status badges: [Live on Web] or [Draft Stage].

Timestamp indicator (e.g., Updated 2m ago).

💻 Phase 2: The Purified Developer IDE Workspace
When a project is actively opened, the dashboard rail vanishes entirely. The viewport morphs into the custom horizontal, two-column sandbox we perfected.

1. The Rigid 48px Header Strip (Full Width)
Left-Aligned (Project Identity):

⚡ SHANGO absolute icon branding.

| Subtle vertical line divider.

Project Name Dropdown: Displays active project title (Lagos Health Portal) with a dropdown click to instantly switch between alternative projects.

Version Tag Pill: A small monochrome pill element (v1.0.4) indicating the active save state.

Right-Aligned (Deployment Panel):

[Share] Button: Translucent outline button opening a quick-copy URL link modal for preview access.

[Deploy to Web] Button: Pure solid high-contrast white button with black text. Instantly builds and pushes the app live.

2. The Left Panel: Orchestration Rail (Fixed 380px Width)
Horizontal View Switcher (Top of panel):

[Chat] Tab (Active): The continuous chronological feature log.

[History] Tab: A visual rollback timeline showing every version patch generated. Clicking an entry lets the developer revert the layout back in time.

[Files] Tab: A clean directory tree view (/src, /components, /styles) allowing manual exploration of the underlying architecture.

The Blueprint Conversation Stream:

A clean, scrolling monochrome log showing user system prompts and SHANGO system responses. No emojis, no chatty conversational fluff—strictly execution feedback.

The Bottom Forge Input Dock (Pinned to Base):

A clean text area box seamlessly integrated into the charcoal #131314 rail.

Contains the core functional command string: "What should SHANGO build next?"

Action shortcuts on the bottom bar: [📎 Attach Asset], [🎙️ Voice Macro].

3. The Right Panel: The Compiler Stage (Flex-1 Responsive Width)
The Control Sub-Header:

Left: Device preview viewport toggles: [🖥️ Desktop], [📱 Mobile], [📟 Tablet].

Right: View modes: [Preview] (Interactive hot-reloaded layout) and [Code View] (Read-only clean source inspector).

The Centered Sandbox Container:

A deep #0d0d0e backdrop environment.

The generated app floats beautifully inside a mock browser window container (border-radius: 14px, clean hairline stroke rgba(255,255,255,0.06), expansive drop shadow).

Features smooth internal horizontal and vertical layout scroll parameters.

⚡ The Ultimate Master Prompt for the Figma Agent
When you are ready to enforce this full system identity inside your Figma design workspace, copy and paste this exact comprehensive prompt to standardise your application layers:

Markdown
### SHANGO Complete Product System Architecture Execution

Systematically map out the absolute end-to-end interface for SHANGO, establishing a pure, high-end Developer OS layout. Ensure NO mention of "AI", "LLM", or "Artificial Intelligence" exists anywhere in the text or interface values. Everything operates under a premium, native developer system identity.

1. GLOBAL INFRASTRUCTURE STATES (Dashboard Views):
   - Design the primary unauthenticated landing page featuring a full edge-to-edge live cosmic/lightning background, centered Forge input dock, and minimalist header navigation containing ONLY [⚡ SHANGO], [Log in], and [Get started].
   - Design the authenticated launch hub layout using the nested app frame: a fixed global sidebar rail on the left (Logo, Forge Hub, Project Vault, Templates, Connectors, Settings) and a beautifully contained, rounded scrollable workspace canvas on the right holding the active projects hub box.

2. PROJECT HUB MODULE BOX:
   - Below the main dashboard omnibox fold, group the project arrays into a clean sub-nav frame (`border-radius: 12px`, `border: 1px solid rgba(255,255,255,0.06)`).
   - Render horizontal tabs: [🔍 Search], [My projects], [Recently viewed], [SHANGO templates] aligned left, and a clean [Browse all →] text button aligned right. All project preview elements must stack in horizontal scroll rows inside this frame.

3. PURIFIED IDE WORKSPACE STATE (Active Project View):
   - Completely remove the dashboard navigation sidebar rail. Set layout view to edge-to-edge fullscreen inside a master container (`border-radius: 20px`, `border: 1px solid rgba(255,255,255,0.05)`, `overflow: hidden`).
   - Create a rigid 48px top control header bar stretching full horizontal width: Left aligns [⚡ SHANGO | Project Title Dropdown | Version Pill]; Right aligns [Share] outline action and [Deploy to Web] solid high-contrast white button.
   - Separate the lower container into two synchronized panels:
     - Left Panel (380px fixed width, #131314 surface): Top row includes [Chat], [History], [Files] toggle tabs. Pinned to the absolute base is the text input prompt window displaying the placeholder: "What should SHANGO build next?"
     - Right Panel (Flex-1 wide, #0d0d0e immersive backdrop): Centered perfectly within this stage is the running mock app viewport, cleanly enclosed in a sharp desktop browser card layout layout.

4. MONOCHROME DESIGN SYSTEM SPECIFICATIONS:
   - Strict palette adherence: Backgrounds use deep obsidian (#0a0a0a, #0d0d0e); panels use solid dark charcoal (#131314); text buttons use crisp off-whites and silvers.
   - All panel borders and structural layout divider strokes must use a highly refined, low-profile hairline value: `1px solid rgba(255, 255, 255, 0.04)`.