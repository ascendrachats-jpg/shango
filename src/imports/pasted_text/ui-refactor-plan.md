### SHANGO Engineering — Unified V1 Workspace & Interface Realignment

Execute a comprehensive, non-destructive UI/UX refactoring pass across the entire active platform framework. Eliminate layout redundancies, fix component alignment collisions, and implement strict, lightweight structural patterns to align the system with elite software standards.

---

### 1. SINGLE TOP NAVIGATION & GLOBAL ACCOUNT ANCHOR (Header Realignment)
- ELIMINATE REDUNDANT LOGOS & DROPDOWNS:
  * Completely remove the "⚡ SHANGO" logo text asset from the top-left corner of the workspace view.
  * Completely remove the redundant "[v1 ▾]" dropdown button sitting next to the project title cluster (the left panel's History stream handles this timeline).
- GLOBAL ACCOUNT LEFT-ANCHORING:
  * Relocate the [Notification Bell] and the perfectly rounded [Profile Avatar Icon] away from the right side of the screen and anchor them directly into the absolute top-left corner frame. 
  * Position them as a local user command deck, followed by a sharp vertical hairline divider (`1px solid rgba(255,255,255,0.04)`), then the [Project Name Title Header].
- LOCAL SPATIAL CONTROLS:
  * Place the "[Hide Chat]" toggle button exactly where the old "v1" dropdown button used to sit, acting as a clean spatial controller directly adjacent to the project identity.
- COMPACT BREADCRUMB STRIP:
  * Clamp a strict maximum-width restriction onto the central browser mock address bar area. The pathing address (`shango.local/hospital-appointment-system`) must render as a compact, elegant breadcrumb track rather than sprawling loosely across the horizon.

---

### 2. INTUITIVE WORKSPACE CONTROLS & DUAL-STATE INPUT (The Smart Omnibox)
- PLAN VS. BUILD INTERACTIVE TOGGLE:
  * Integrally embed a dual-state capsule toggle switch directly inside the central translucent Omnibox (and the workspace prompting input decks), positioned cleanly next to the attachment and submit action icons.
  * [Plan Mode]: A low-impact, silver-glowing layout setting for architectural exploration, posing data queries, and brainstorming mapping states without consuming high-tier token compilation resources.
  * [Build Mode]: A high-contrast, active engineering setting that commands SHANGO to aggressively mutate source files, compile components, and execute database schemas.
- COGNITIVE FOCUS STATE (Atmospheric Blur):
  * When a user actively clicks inside or types within the Omnibox input field, apply a smooth background backdrop-filter transition across the entire remaining interface layout (`backdrop-filter: blur(8px); transition: all 0.25s ease;`). The Omnibox must remain flawlessly sharp, high-contrast, and isolated in crisp structural focus.
- THE WELCOME HERO PURGE:
  * Move the slogan string "DESCRIBE. BUILD. SHIP." away from the central cluster and pin it cleanly to the absolute top-center of the entire screen view as a refined, low-opacity technical header utility.
  * Completely remove the text blocks "AFRICA'S AI APPLICATION BUILDER" and the duplicate floating bolt icon layer to maximize clean negative space.

---

### 3. MANUAL CONTROL SIDEBAR RAIL & COMPACT AVATAR
- LOVABLE-INSPIRED CLICK TOGGLE OVERHAUL:
  * Completely deactivate and remove the automatic "slide-on-hover" sidebar expansion trigger. The navigation rail must remain statically anchored to prevent unwanted layout shifting.
  * Place a dedicated manual collapse/expand layout icon (such as a double chevron `«` or architectural toggle) at the top of the sidebar rail so the engineer determines exactly when the navigation states change.
- MINIATURIZED PROFILE ANCHOR CAP:
  * Convert the bulky, large boxed profile button container at the base of the sidebar rail into a tight, perfectly circular asset container (`border-radius: 50%`) centered cleanly over the vertical rail axis.
- FLOATING DROPUP MEMORY DECK:
  * Ensure that when the sidebar is collapsed and the user interacts with the Profile Avatar, the resulting popover sheet detaches cleanly from the rail edge. It must project outward as an independent floating glass modal (`position: absolute; left: calc(100% + 10px); z-index: 999; width: 240px;`) with zero layout cramming.
  * Keep the contents strictly limited to: User Identity metrics, a thin hairline Resource Allocation progress bar tracking compilation tokens, and direct links for `[Account Settings]`, `[Keyboard Shortcuts ⌘]`, and `[Sign Out]`.

---

### 4. NON-DESTRUCTIVE MODALS & DRAWERS (Zero Context Shock)
- THE CONNECTORS UTILITY DRAWER:
  * Completely eliminate the full-viewport page redirection routine when users click on the "Connectors" tab. The operator must never lose their workspace environment context.
  * Rebuild the entire integration directory (GitHub, Supabase, Paystack, Vercel) to slide into view gracefully as a non-destructive right-side overlay utility drawer (`width: 480px`).
  * Utilize an ultra-premium glass canvas setting: `background: rgba(19, 19, 20, 0.75); backdrop-filter: blur(24px); border-left: 1px solid rgba(255, 255, 255, 0.05);`. Stack integrations cleanly as compact component tiles featuring a micro-monochrome glyph, state indicators, and an explicit `[Manage]` toggle action.